spring:
  application:
    name: limits-service
  profiles:
    active: qa
  cloud.config.uri: http://localhost:8888

management:
  endpoints:
    web:
      exposure.include: '*'
  security:
    enabled: false
