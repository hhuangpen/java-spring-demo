spring:
  application:
    name: netflix-zuul-api-gateway-server

server:
  port: 8765

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

#zuul:
#   ribbon:
#      eager-load:
#         enabled: true